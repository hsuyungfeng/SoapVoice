---
phase: 01-infrastructure
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - docker-compose.yml
  - Dockerfile
  - .gitignore
autonomous: true
requirements:
  - SETUP-04
  - SETUP-05
must_haves:
  truths:
    - "Docker 與 Docker Compose 可正常運行"
    - "Git repository 已設定並可追蹤版本"
  artifacts:
    - path: docker-compose.yml
      provides: "Docker Compose  orchestration"
      contains: "version:"
    - path: Dockerfile
      provides: "容器化配置"
      contains: "FROM python"
    - path: .gitignore
      provides: "Git 忽略規則"
      contains: "__pycache__"
  key_links:
    - from: docker-compose.yml
      to: "Docker daemon"
      via: "docker compose up"
    - from: .gitignore
      to: "Git"
      via: "git status 驗證"
---

<objective>
建立 Docker 環境配置與 Git 版本控制設定。

目的：確保容器化部署就緒，版本控制正常運作
產出：docker-compose.yml、Dockerfile、.gitignore
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>建立 Docker 環境配置</name>
  <files>
    - docker-compose.yml
    - Dockerfile
  </files>
  <action>
1. 建立 Dockerfile：
   - FROM python:3.11-slim
   - 安裝必要系統依賴 (cuda-runtime, ffmpeg 等)
   - 設定工作目錄
   - 複製 pyproject.toml
   - 使用 uv 安裝依賴

2. 建立 docker-compose.yml：
   - 定義 services: api, whisper, vllm
   - 設定 GPU 資源共享
   - 設定網路配置
   - volume 掛載

3. 驗證 Docker 可運行：
   - docker --version
   - docker compose version
  </action>
  <verify>
docker --version && docker compose version
  </verify>
  <done>Docker 環境配置完成，可執行 docker compose config 驗證語法</done>
</task>

<task type="auto">
  <name>建立 Git 版本控制設定</name>
  <files>.gitignore</files>
  <action>
1. 建立 .gitignore，包含：
   - Python: __pycache__/, *.pyc, .venv/, venv/
   - 環境: .env, .env.local
   - Docker: *.log, .docker/
   - IDE: .vscode/, .idea/
   - 模型: models/, cache/

2. 驗證 Git 狀態：
   - git status 確認無意外檔案
   - git log 確認提交歷史
  </action>
  <verify>
git status --porcelain
  </verify>
  <done>.gitignore 已建立，Git 可正常追蹤版本</done>
</task>

</tasks>

<verification>
- [ ] docker --version 顯示版本
- [ ] docker compose version 顯示版本
- [ ] docker-compose.yml 語法正確
- [ ] Dockerfile 可建構
- [ ] .gitignore 包含常見忽略規則
- [ ] git status 顯示乾淨或預期內的狀態
</verification>

<success_criteria>
Docker Compose 配置完成，Git 版本控制正常運作
</success_criteria>

<output>
完成後建立 .planning/phases/01-infrastructure/01-03-SUMMARY.md
</output>
