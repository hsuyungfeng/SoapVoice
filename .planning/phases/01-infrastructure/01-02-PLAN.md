---
phase: 01-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .env
  - pyproject.toml
autonomous: false
requirements:
  - SETUP-02
  - SETUP-03
user_setup:
  - service: nvidia-driver
    why: "GPU 運算必需"
    env_vars: []
    dashboard_config:
      - task: "安裝 NVIDIA 驅動"
        location: "系統偏好設定 -> 軟體更新 -> 附加驅動 或 手動下載 .run"
      - task: "驗證 CUDA Toolkit"
        location: "nvidia-smi 應顯示 CUDA Version"
must_haves:
  truths:
    - "NVIDIA 驅動與 CUDA 12.1+ 已安裝並驗證"
    - "uv + Python 3.11 環境可運作"
  artifacts:
    - path: .env
      provides: "環境變數配置"
      contains: "CUDA_PATH"
    - path: pyproject.toml
      provides: "Python 專案配置"
      contains: "python = \">=3.11\""
  key_links:
    - from: .env
      to: "系統 CUDA"
      via: "CUDA_PATH 變數"
    - from: pyproject.toml
      to: "uv"
      via: "依賴管理"
---

<objective>
驗證 GPU 驅動與 CUDA 環境，安裝 uv 與 Python 3.11 開發環境。

目的：確保 GPU 可用於深度學習運算，建立 Python 開發環境
產出：.env 配置、pyproject.toml
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify">
  <what-built>GPU 驅動與 CUDA 12.1+ 環境</what-built>
  <how-to-verify>
1. 執行 nvidia-smi 確認驅動已安裝
2. 檢查 CUDA Version ≥ 12.1
3. 執行 nvcc --version 確認 CUDA Toolkit
4. 驗證範例程式可編譯執行 (optional)
  </how-to-verify>
  <resume-signal>輸入 "approved" 或描述問題</resume-signal>
</task>

<task type="auto">
  <name>建立 uv + Python 3.11 環境</name>
  <files>
    - .env
    - pyproject.toml
  </files>
  <action>
1. 建立 .env 檔案，包含：
   - CUDA_PATH=/usr/local/cuda
   - PYTHON_VERSION=3.11
   - 專案環境變數

2. 建立 pyproject.toml，包含：
   - python = ">=3.11"
   - 專案名稱 soapvoice
   - uv 相關設定

3. 使用 uv 建立虛擬環境（若 uv 已安裝）
   - uv venv
   - uv sync

注意：若 uv 未安裝，在驗證 checkpoint 中處理
  </action>
  <verify>
- [ -f .env ] && [ -f pyproject.toml ]
- cat .env | grep CUDA_PATH
- cat pyproject.toml | grep python
  </verify>
  <done>.env 與 pyproject.toml 已建立，CUDA_PATH 已設定</done>
</task>

</tasks>

<verification>
- [ ] nvidia-smi 可執行且顯示 GPU
- [ ] CUDA Version ≥ 12.1
- [ ] .env 包含 CUDA_PATH
- [ ] pyproject.toml 指定 python >=3.11
</verification>

<success_criteria>
GPU 環境可用，Python 3.11 專案配置完成
</success_criteria>

<output>
完成後建立 .planning/phases/01-infrastructure/01-02-SUMMARY.md
</output>
