---
phase: 02-ai
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/api/websocket.py, src/asr/stream_transcriber.py, config/websocket.yaml]
autonomous: true
requirements: [ASR-03]

must_haves:
  truths:
    - "WebSocket 連線可建立"
    - "即時音頻串流可處理"
    - "轉文字結果可即時回傳"
  artifacts:
    - path: "src/api/websocket.py"
      provides: "WebSocket 端點"
      min_lines: 60
    - path: "src/asr/stream_transcriber.py"
      provides: "串流辨識器"
      min_lines: 50
    - path: "config/websocket.yaml"
      provides: "WebSocket 配置"
  key_links:
    - from: "src/api/websocket.py"
      to: "src/asr/stream_transcriber.py"
      via: "依賴注入"
      pattern: "StreamTranscriber"
---

<objective>
建立 WebSocket 即時串流 pipeline，支援即時語音轉文字

Purpose: 讓前端可以即時傳送音頻並獲得轉文字結果
Output: src/api/websocket.py, src/asr/stream_transcriber.py
</objective>

<context>
@.planning/phases/02-ai/02-01-PLAN.md (Whisper 模型已部署)
@.planning/phases/02-ai/02-CONTEXT.md (效能目標: <500ms RTF<0.3)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 建立串流辨識器</name>
  <files>src/asr/stream_transcriber.py</files>
  <action>
建立 StreamTranscriber 類:
- __init__(whisper_model: WhisperModel)
- start_stream() -> None
- process_chunk(audio_chunk: bytes) -> str  # 即時處理音頻塊
- end_stream() -> dict  # 返回最終結果
- 支援 chunked 處理模式
- 即時返回目前已辨識文字

使用 Faster-Whisper 的 streaming 模式
  </action>
  <verify>python -c "from src.asr.stream_transcriber import StreamTranscriber; print('StreamTranscriber imported')"</verify>
  <done>StreamTranscriber 類可正確匯入</done>
</task>

<task type="auto">
  <name>Task 2: 建立 WebSocket 端點</name>
  <files>src/api/websocket.py</files>
  <action>
建立 WebSocket 端點:
- /ws/transcribe: 即時語音串流端點
- 接受 WebSocket 連線
- 接收音頻數據 (JSON 或 binary)
- 即時回傳轉文字結果
- 支援訊號控制 (start, chunk, end)
- 錯誤處理與連線管理

使用 FastAPI 的 WebSocket 支援
  </action>
  <verify>python -c "from src.api.websocket import router; print('WebSocket router imported')"</verify>
  <done>WebSocket 端點可正確匯入</done>
</task>

<task type="auto">
  <name>Task 3: 建立 WebSocket 配置</name>
  <files>config/websocket.yaml</files>
  <action>
建立 config/websocket.yaml:
- host: "0.0.0.0"
- port: 8001
- max_connections: 10
- chunk_size: 1024  # 音頻塊大小
- ping_interval: 30
- buffer_size: 4096
  </action>
  <verify>python -c "import yaml; c = yaml.safe_load(open('config/websocket.yaml')); assert 'host' in c"</verify>
  <done>WebSocket 配置正確建立</done>
</task>

</tasks>

<verification>
- WebSocket 端點可正確建立連線
- 串流處理可即時回傳結果
- 連線管理正常運作
</verification>

<success_criteria>
- WebSocket 即時串流 pipeline 完成 (ASR-03 完成)
- 效能目標: RTF < 0.3 (每 100ms 音頻處理 < 30ms)
- 未來 Phase 4 會整合到完整 API
</success_criteria>

<output>
完成後建立 `.planning/phases/02-ai/02-03-SUMMARY.md`
</output>
