---
phase: 02-ai
plan: 04
type: execute
wave: 2
depends_on: [02-01, 02-02, 02-03]
files_modified: [src/llm/vllm_engine.py, config/llm.yaml, src/api/routes.py, tests/test_llm.py]
autonomous: true
requirements: [LLM-02, LLM-03]

must_haves:
  truths:
    - "GLM-4.7-Flash 可透過 vLLM 載入"
    - "OpenAI 相容 API 可運作"
    - "推理延遲 < 2s @ 95th percentile"
  artifacts:
    - path: "src/llm/vllm_engine.py"
      provides: "vLLM 引擎封裝"
      min_lines: 60
    - path: "config/llm.yaml"
      provides: "LLM 配置"
      contains: "glm_4_flash"
    - path: "src/api/routes.py"
      provides: "API 路由"
      min_lines: 40
  key_links:
    - from: "src/api/routes.py"
      to: "src/llm/vllm_engine.py"
      via: "依賴呼叫"
      pattern: "VLLMEngine"
---

<objective>
部署 GLM-4.7-Flash 推理引擎，建立 OpenAI 相容 API

Purpose: 為 Phase 3 醫療 NLP 引擎提供語言模型能力
Output: src/llm/vllm_engine.py, config/llm.yaml, src/api/routes.py
</objective>

<context>
@.planning/phases/02-ai/02-CONTEXT.md (使用者決策: GLM-4.7-Flash 為主模型)
@.planning/phases/02-ai/02-01-PLAN.md (Whisper 已部署)
@.planning/ROADMAP.md (LLM-02, LLM-03)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 vLLM 依賴</name>
  <files>pyproject.toml</files>
  <action>
在 pyproject.toml 的 dependencies 中加入:
- "vllm>=0.4.0"
- "openai>=1.10.0"

使用 uv sync 更新依賴
注意: vLLM 需要 CUDA 環境，確保 Phase 1 環境正確
  </action>
  <verify>uv sync 成功，python -c "import vllm; print(vllm.__version__)"</verify>
  <done>vLLM 依賴已安裝</done>
</task>

<task type="auto">
  <name>Task 2: 建立 vLLM 引擎封裝</name>
  <files>src/llm/vllm_engine.py</files>
 <action>
建立 VLLMEngine 類:
- __init__(model: str = "glm-4-flash", tensor_parallel_size: int = 1)
- load_model() -> None
- generate(prompt: str, **kwargs) -> str
- chat(messages: list[dict], **kwargs) -> dict
- 支援 OpenAI 相容格式
- 整合 streaming 支援

建立 src/llm/__init__.py 匯出 VLLMEngine
確保 GPU 記憶體管理正確
  </action>
 <verify>python -c "from src.llm import VLLMEngine; print('VLLMEngine imported')"</verify>
 <done>VLLMEngine 可正確匯入</done>
</task>

<task type="auto">
  <name>Task 3: 建立 LLM 配置檔</name>
 <files>config/llm.yaml</files>
 <action>
建立 config/llm.yaml:
- glm_4_flash:
    model_name: "THUDM/glm-4-flash"
    tensor_parallel_size: 1
    gpu_memory_utilization: 0.9
    max_num_seqs: 256
    max_model_len: 32768
    dtype: "auto"
    quantization: null
    trust_remote_code: true

- api:
    base_url: "http://localhost:8000/v1"
    api_key: null  # 內網暫無認證
  </action>
 <verify>python -c "import yaml; c = yaml.safe_load(open('config/llm.yaml')); assert 'glm_4_flash' in c"</verify>
 <done>LLM 配置正確建立</done>
</task>

<task type="auto">
  <name>Task 4: 建立 API 路由</name>
 <files>src/api/routes.py</files>
 <action>
建立 /v1/chat/completions 端點:
- POST /v1/chat/completions
- 接受 OpenAI 相容格式的 messages
- 呼叫 VLLMEngine.generate()
- 返回 OpenAI 相容格式回應

建立 /health 端點用於健康檢查
建立 src/api/__init__.py
  </action>
 <verify>python -c "from src.api.routes import router; print('API router imported')"</verify>
 <done>API 路由可正確匯入</done>
</task>

<task type="auto">
  <name>Task 5: 建立 LLM 測試</name>
 <files>tests/test_llm.py</files>
 <action>
建立測試檔案:
- test_llm_engine_init: 驗證引擎初始化
- test_llm_config_load: 驗證配置載入
- test_chat_format: 驗證聊天格式正確性
- 注意: 實際模型測試需要模型下載後執行
  </action>
 <verify>pytest tests/test_llm.py -v --collect-only</verify>
 <done>測試可收集，結構正確</done>
</task>

</tasks>

<verification>
- vLLM 依賴可正確安裝
- VLLMEngine 可正確運作
- OpenAI 相容 API 格式正確
- 延遲測試可在部署後執行
</verification>

<success_criteria>
- GLM-4.7-Flash 部署完成 (LLM-02 完成)
- 推理延遲 < 2s @ 95th percentile (LLM-03 完成)
- OpenAI 相容格式可用
- Qwen3-32B 保留為未來選項 (CONTEXT.md 延遲)
</success_criteria>

<output>
完成後建立 `.planning/phases/02-ai/02-04-SUMMARY.md`
</output>
