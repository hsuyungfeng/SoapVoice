---
phase: 02-ai
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pyproject.toml, requirements.txt, src/asr/whisper_model.py, config/models.yaml]
autonomous: true
requirements: [ASR-01, ASR-04]

must_haves:
  truths:
    - "Faster-Whisper large-v3 可載入並進行語音辨識"
    - "模型輸出文字結果正確"
    - "支援中文與英文語音辨識"
  artifacts:
    - path: "src/asr/whisper_model.py"
      provides: "Faster-Whisper 模型封裝"
      min_lines: 50
    - path: "config/models.yaml"
      provides: "模型配置"
      contains: "whisper_large_v3"
    - path: "pyproject.toml"
      provides: "Python 依賴"
      contains: "faster-whisper"
  key_links:
    - from: "src/asr/whisper_model.py"
      to: "faster_whisper"
      via: "import"
      pattern: "import faster_whisper"
---

<objective>
部署 Faster-Whisper large-v3 模型，使系統可進行動音辨識

Purpose: 建立語音轉文字基礎設施，支援 Phase 3 醫療 NLP 處理
Output: src/asr/whisper_model.py, config/models.yaml, 更新 pyproject.toml
</objective>

<context>
@.planning/phases/01-infrastructure/01-03-SUMMARY.md (Docker 環境已設定)
@.planning/ROADMAP.md (Phase 2 目標)
@.planning/phases/02-ai/02-CONTEXT.md (使用者決策: Faster-Whisper large-v3)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 新增 Faster-Whisper 依賴</name>
  <files>pyproject.toml, requirements.txt</files>
  <action>
在 pyproject.toml 的 dependencies 中加入:
- "faster-whisper>=1.0.0"
- "torch>=2.1.0" (CPU 推論用)
- "torchaudio>=2.1.0"

建立 requirements.txt 同步更新
使用 uv sync 更新依賴
  </action>
  <verify>uv sync 成功，python -c "import faster_whisper; print(faster_whisper.__version__)"</verify>
  <done>依賴已安裝，faster_whisper 可匯入</done>
</task>

<task type="auto">
  <name>Task 2: 建立 Whisper 模型封裝類</name>
  <files>src/asr/whisper_model.py</files>
  <action>
建立 WhisperModel 類:
- init(model_id: str = "large-v3", device: str = "cuda") 
- transcribe(audio_path: str) -> dict  # 返回 {text, language, segments}
- 支援中英文自動偵測 (language detection)
- 使用 CTranslate2 優化推論速度
- 建立 src/asr/__init__.py 匯出 WhisperModel

注意: 使用 large-v3 模型，device 參數支援 cuda/cpu
  </action>
  <verify>python -c "from src.asr import WhisperModel; m = WhisperModel(); print('WhisperModel loaded')"</verify>
  <done>WhisperModel 可實例化並呼叫 transcribe()</done>
</task>

<task type="auto">
  <name>Task 3: 建立模型配置檔</name>
  <files>config/models.yaml</files>
  <action>
建立 config/models.yaml:
- whisper:
    model_id: "large-v3"
    device: "cuda"
    compute_type: "float16"
    language: "auto"  # 自動偵測
    task: "transcribe"

確保 config 目錄存在
  </action>
  <verify>python -c "import yaml; c = yaml.safe_load(open('config/models.yaml')); assert 'whisper' in c"</verify>
  <done>config/models.yaml 包含 Whisper 配置</done>
</task>

</tasks>

<verification>
- faster-whisper 套件可正常安裝
- WhisperModel 類可正確運作
- 可處理音頻檔案並輸出文字
</verification>

<success_criteria>
- Faster-Whisper large-v3 可載入 (ASR-01 完成)
- 支援中文與英文語音辨識 (ASR-04 完成)
- WER 測試可執行 (未來 Phase 3 驗證)
</success_criteria>

<output>
完成後建立 `.planning/phases/02-ai/02-01-SUMMARY.md`
</output>
